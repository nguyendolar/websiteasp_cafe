<nav class="sb-topnav navbar navbar-expand navbar-dark bg-dark">
    <a class="navbar-brand ps-3 text-uppercase fw-bold" href="/AdminHome/Index">Cafe Admin</a>

    <button class="btn btn-link btn-sm text-white order-1 order-lg-0 me-4 me-lg-0" id="sidebarToggle">
        <i class="fas fa-bars"></i>
    </button>

    <p class="text-white ms-auto my-auto d-none d-md-block">
        <span class="fw-bold">Bây giờ là:</span> @DateTime.Now.ToString(@"hh\:mm tt")
        <span class="fw-bold">, Ngày:</span> @String.Format("{0:dd-MM-yyyy}", DateTime.Now)
    </p>

    <ul class="navbar-nav ms-auto me-3 me-lg-4">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-white" id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown">
                <i class="fas fa-user fa-fw"></i>
                <span>@(((websitecafe.Models.User)Session["ADMIN"])?.FullName)</span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <li><a class="dropdown-item" href="/AdminAuthen/Logout">Đăng xuất</a></li>
            </ul>
        </li>
    </ul>
</nav>
